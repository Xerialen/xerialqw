# --- LINUX SPECIFIKT ---
# Ignorera binära filer (spelmotorer)
*.bin
*.x86_64

# --- QUAKE DATA ---
# VIKTIGT: Ignorera copyright-skyddat material
id1/pak1.pak
id1/pak0.pak


# Ignorera stora kompilerade kartor (frivilligt, men rekommenderas)
*.bsp
*.lit

# --- SYSTEM & SKRÄP ---
.DS_Store
*.log
*~
*.AppImage
ezquake/.ezquake_history
qw/matchinfo/screenshots/
qw/matchinfo/demos/temp/
ezquake/sb/cache/

# --- Upstream redistributable game packs (huge, re-fetched by the nQuake installer; never in repo history) ---
*.pk3


# --- Session debris (added 2026-05-29) ---
# Demos — match recordings kept locally, not in repo
*.mvd
*.mvd.gz
# config.cfg is tracked; its backups are not
ezquake/configs/config.cfg.*
*.bak
# Quarantined old engine binaries
_quarantine/
# HUD-capture / huditor runner artifacts (hud-wizard archived; ezhud B-capture runs on servexeri)
render-helper.bat
qw/render.cfg
qw/huditor-undertest.cfg
ezquake/demo_probe.cfg
ezquake/demo_shot.cfg
